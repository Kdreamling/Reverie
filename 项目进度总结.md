# Reverie 项目进度总结

> 日期：2026-03-10
> 状态：前端基础架构完成，核心功能已对接后端

---

## 一、项目概述

Reverie 是一个极简风格的 AI 聊天 Web 应用，部署路径为 `/chat/`，后端服务运行在 `https://kdreamling.work`。前端使用 React + TypeScript + Vite 构建，设计语言以克莱因蓝（`#002FA7`）为核心色，追求深邃夜空 + 极简排版的视觉风格。

---

## 二、已完成内容

### 1. 工程基础

| 文件 | 说明 |
|------|------|
| `package.json` | React 19 + Vite 6 + TypeScript 5.7 |
| `vite.config.ts` | `base: '/chat/'`，`appType: 'spa'`，代理 `/api` 和 `/v1` 到后端 |
| `tsconfig.*.json` | 标准 TypeScript 项目配置 |
| `.env.development` | `VITE_API_BASE_URL=/api`（走 Vite 代理，绕 CORS）|
| `.gitignore` | node_modules、dist、.env 等 |
| `src/index.css` | Tailwind CSS 4 + highlight.js 主题 + `.md-content` prose 样式 |

**依赖库：**
- `tailwindcss` + `@tailwindcss/vite`（CSS 框架）
- `zustand`（状态管理）
- `react-router-dom` v7（路由）
- `react-markdown` + `rehype-highlight`（Markdown 渲染 + 代码高亮）
- `lucide-react`（图标）

---

### 2. 页面与组件

#### LoginPage（`src/pages/LoginPage.tsx`）
- 全屏克莱因蓝渐变背景（`#001245 → #002FA7 → #001650`）
- 50 颗固定位置星星（SVG，opacity 0.25–0.55）
- 半闭眼睛 SVG 徽章（圆形边框，stroke-width 1，上眼睑弧线 + 下眼睑 + 瞳孔）
- 大标题"R E V E R I E"（Georgia serif，text-5xl，letterSpacing 0.3em）
- 副标题（opacity 0.5，fadeIn 动画延迟 0.4s）
- 密码输入框（透明背景，白色边框，focus 变亮）
- "Enter Reverie"按钮（loading 状态显示"Entering…"，disabled）
- 错误提示（红色小字，401 → "Incorrect password."，其他 → "Connection failed."）
- 登录成功后 `navigate('/')` 跳转聊天页

#### ChatPage（`src/pages/ChatPage.tsx`）

**左侧侧栏（260px，`#0a1a3a`）：**
- "✦ REVERIE" 品牌标题 + Plus 按钮（新建会话）
- 会话列表：按 `created_at` 分组（Today / Yesterday / Previous 7 Days）
- 选中态：左边 2px 克莱因蓝竖线 + `rgba(0,47,167,0.3)` 背景
- Hover 时右侧显示 X 删除按钮（stopPropagation，调 deleteSession）
- Settings 入口（底部）

**右侧聊天区（flex-1，`#fafbfd`）：**
- 顶栏（56px）：当前模型名 + `<select>` 切换器（切换时调 updateSessionAPI）
- 消息区（max-width 800px 居中）：
  - 欢迎页：无消息时显示 ✦ + REVERIE（居中占位）
  - 用户消息："D" 圆形头像 + 纯文本
  - AI 消息：✦ 符号 + Markdown 渲染（react-markdown + rehype-highlight）
  - Thinking 区域：克莱因蓝左竖线 + `#f0f3fa` 背景，可折叠，流式时默认展开
  - 流式输出中：底部实时追加 ThinkingBlock（open）和 MarkdownContent
  - 等待响应时显示蓝色脉冲圆点（blink 动画）
  - 自动滚动到底部（messages.length / currentText / currentThinking 变化时）
- 输入区（`#f2f4fa`）：
  - Textarea 自动增高（最多 5 行）
  - Enter 发送，Shift+Enter 换行
  - 流式输出中禁用输入框和发送按钮
  - 未选中 session 时禁用并提示

#### AuthGuard（`src/components/AuthGuard.tsx`）
- 检查 `authStore.isLoggedIn`
- 未登录 → `<Navigate to="/login" replace />`
- 已登录 → `<Outlet />`（渲染子路由）

---

### 3. 路由配置（`src/App.tsx`）

```
BrowserRouter basename="/chat"
├── /login          → LoginPage
├── /               → ChatPage（AuthGuard 保护）
├── /:sessionId     → ChatPage（AuthGuard 保护）
└── *               → Navigate to="/"
```

启动时调 `authStore.init()`（从 localStorage 恢复 token），监听 `auth:unauthorized` 事件 → `logout()`。

---

### 4. API 层

#### `src/api/client.ts`
- fetch 封装，支持 GET / POST / PATCH / DELETE
- 自动注入 `Authorization: Bearer {token}`
- 401 时广播 `auth:unauthorized` 自定义 DOM 事件
- 支持 204 No Content（返回 `undefined`）
- 错误时抛出 `ApiError(status, message)`

#### `src/api/auth.ts`
- `loginAPI(password)` → POST `/auth/login` → `{ token, expires_at }`

#### `src/api/sessions.ts`
- `fetchSessionsAPI()` → GET `/sessions`
- `createSessionAPI(scene_type, model)` → POST `/sessions`
- `deleteSessionAPI(id)` → DELETE `/sessions/{id}`
- `updateSessionAPI(id, data)` → PATCH `/sessions/{id}`

#### `src/api/chat.ts`
- `fetchMessagesAPI(sessionId)` → GET `/sessions/{id}/messages`
- `streamChat(sessionId, model, content, token)` → POST `/v1/chat/completions`，返回原始 `Response`

---

### 5. 状态管理

#### `authStore.ts`
- `token / isLoggedIn`
- `init()`：从 localStorage 恢复
- `login(password)`：调 API，存 token
- `logout()`：清 token，isLoggedIn = false → AuthGuard 触发跳转

#### `sessionStore.ts`
- `sessions[] / currentSession / loading`
- `fetchSessions()`：拉列表，按 updated_at 倒序排列
- `createSession(scene_type, model)`：创建并自动选中
- `selectSession(id)`：切换当前会话
- `deleteSession(id)`：删除，自动选中第一条
- `getGroup(dateStr)`：日期 → today / yesterday / previous
- `formatSessionTime(dateStr)`：格式化显示时间

#### `chatStore.ts`
- `messages[] / isStreaming / currentThinking / currentText`
- `loadMessages(sessionId)`：加载历史，兼容 `[]` 和 `{ data: [] }` 两种响应格式
- `sendMessage(sessionId, model, content)`：
  1. 本地立即添加用户消息
  2. 调 `streamChat` 发起流式请求
  3. 逐行解析 SSE（`data: {...}`）
  4. `thinking_delta` → 追加 currentThinking
  5. `text_delta` → 追加 currentText
  6. `done` → 组装 assistantMsg，清空流式状态
  7. finally 安全兜底：强制 `isStreaming = false`
- `clearMessages()`：切换 session 时清空

---

### 6. 开发环境配置

```
本地请求 /api/* → Vite 代理 → https://kdreamling.work/api/*
本地请求 /v1/*  → Vite 代理 → https://kdreamling.work/v1/*
```

`appType: 'spa'` 确保 `/chat/*` 子路径刷新时返回 `index.html`（SPA 路由 fallback）。

---

## 三、尚未实现的内容

### 功能层面

| 功能 | 说明 |
|------|------|
| 会话标题自动更新 | 发送第一条消息后，后端会根据内容生成标题，前端尚未轮询或接收更新 |
| 多轮对话上下文 | `sendMessage` 目前只发送当前消息，未将历史 messages 一起传给后端（body 里 messages 数组只有一条）|
| 场景类型选择 | 新建会话时写死 `scene_type: 'daily'`，未提供 UI 选择 |
| Settings 页面 | 侧栏底部的 Settings 按钮无功能，点击无响应 |
| 消息流式中断 | 无"停止生成"按钮，流式输出途中无法中断 |
| 错误提示 UI | API 错误（非 401）在聊天区没有视觉反馈 |
| 消息复制按钮 | AI 回复没有一键复制功能 |
| 代码块语言标签 | 代码高亮有，但无语言名称显示和复制按钮 |
| 移动端适配 | 布局未针对小屏幕优化，侧栏在移动端不可折叠 |
| 深色模式 | 无 dark mode 支持 |

### 工程层面

| 项目 | 说明 |
|------|------|
| highlight.js 按需加载 | 当前全量打包，bundle 550KB，应按语言 tree-shake |
| Token 过期检测 | `expires_at` 字段已返回但未使用，没有到期前自动刷新或提示 |
| 请求取消 | 切换 session 时正在进行的流式请求不会被中断（AbortController 未实现）|
| 持久化 currentSession | 刷新页面后 currentSession 丢失（sessionStore 无持久化）|
| 测试 | 无单元测试或 e2e 测试 |
| 生产部署配置 | 生产环境的 nginx/反向代理 SPA fallback 需额外配置 |
